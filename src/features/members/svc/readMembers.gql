query Members($skip: Int, $take: Int, $filter: MemberFilterInput, $order: [MemberSortInput!]) {
	members(skip: $skip, take: $take, where: $filter, order: $order) {
		pageInfo {
			hasNextPage
			hasPreviousPage
		}
		totalCount
		items {
			additionalInformation
			address {
				rootElement
			}
			category
			commencementYear
			createdBy
			employeeDetails {
				rootElement
			}
			ghanaCardNumber
			createdOn
			description
			email
			id
			membershipDeclaration {
				rootElement
			}

			name
			operationalArea
			organization
			phoneNumber
			productMaterialCategory
			reason
			redgNo
			registeredBy {
				rootElement
			}
			revision
			status
			updatedBy
			updatedOn
		}
	}
}
